package scanner

// This package hosts concrete rules.Scanner implementations (Docker, node_modules, etc.).
// Phase 1 only establishes the interface + engine integration; Workstream C adds implementations.
